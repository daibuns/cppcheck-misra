{
  "name": "cppcheck-misra",
  "displayName": "Cppcheck MISRA",
  "description": "Run cppcheck with MISRA C:2012, MISRA C:2023, MISRA C++:2008, and MISRA C++:2023 rules on C/C++ files",
  "version": "0.1.0",
  "publisher": "daibun",
  "engines": {
    "vscode": "^1.106.1"
  },
  "categories": [
    "Linters"
  ],
  "activationEvents": [
    "onLanguage:c",
    "onLanguage:cpp"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "cppcheck-misra.run",
        "title": "Cppcheck MISRA: Run on current file"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Cppcheck MISRA",
      "properties": {
        "cppcheck-misra.cppcheckPath": {
          "type": "string",
          "default": "cppcheck",
          "description": "Path to the cppcheck executable"
        },
        "cppcheck-misra.enableOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Run cppcheck MISRA automatically on file save"
        },

        "cppcheck-misra.cStd": {
          "type": "string",
          "enum": ["c89", "c99", "c11"],
          "default": "c99",
          "description": "C language standard passed to cppcheck via --std=<value> for C files."
        },
        "cppcheck-misra.cppStd": {
          "type": "string",
          "enum": ["c++03", "c++11", "c++14", "c++17", "c++20"],
          "default": "c++17",
          "description": "C++ language standard passed to cppcheck via --std=<value> for C++ files."
        },
        "cppcheck-misra.reportType": {
          "type": "string",
          "enum": ["auto", "misra-c-2012", "misra-c-2023", "misra-cpp-2008", "misra-cpp-2023"],
          "default": "auto",
          "description": "MISRA report type: auto (detect by file type), misra-c-2012, misra-c-2023, misra-cpp-2008, or misra-cpp-2023"
        },

        "cppcheck-misra.severityMapping.mandatory": {
          "type": "string",
          "enum": ["error", "warning", "information", "hint"],
          "default": "error",
          "description": "Diagnostic severity for MISRA Mandatory violations"
        },
        "cppcheck-misra.severityMapping.required": {
          "type": "string",
          "enum": ["error", "warning", "information", "hint"],
          "default": "warning",
          "description": "Diagnostic severity for MISRA Required violations"
        },
        "cppcheck-misra.severityMapping.advisory": {
          "type": "string",
          "enum": ["error", "warning", "information", "hint"],
          "default": "information",
          "description": "Diagnostic severity for MISRA Advisory violations"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.106.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.46.3",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}